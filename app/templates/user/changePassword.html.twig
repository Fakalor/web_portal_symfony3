{% include 'default/_form.html.twig' with {
    page_title: 'user_change_password'|trans,
    attr: {
        method: 'POST',
        action: url('user_change_password'),
    },
    back_to_list_path: 'user_index',
} %}