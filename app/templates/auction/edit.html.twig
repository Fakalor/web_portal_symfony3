{% include 'default/_form.html.twig' with {
    page_title: 'title.auction_edit',
    page_title_param: {'%id%': auction.id},
    attr: {
        method: 'PUT',
        action: url('auction_edit', {'id': auction.id}),
    },
    back_to_list_path: 'auction_index',
} %}