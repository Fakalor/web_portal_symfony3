{% include 'default/_form.html.twig' with {
    page_title: 'title.deal_new',
    page_title_param: {'%id%': auction.id},
    attr: {
        method: 'POST',
        action: url('deal_new', {'id': auction.id})
    },
    back_to_list_path: 'auction_start_page',
} %}